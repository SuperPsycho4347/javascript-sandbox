let x;

// Array Literal
const numbers = [12, 14, 17, 35];
const mixed = [12, 'Hello', true, null]

// Array Constructor
const fruits = new Array('Apple', 'Orange','Guava');

x = numbers[0];

x = numbers[0] + numbers[3];

fruits[2] = 'pair'

console.log(fruits);